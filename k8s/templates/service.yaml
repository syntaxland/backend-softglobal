apiVersion: v1
kind: Service
metadata:
  name: backend-softglobal-service
  labels:
    app: backend-softglobal
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: backend-softglobal
